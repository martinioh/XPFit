<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xpfit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewUsersProfile"></activity>
        <activity android:name=".ViewPastCardioActivity" />
        <activity android:name=".ViewPastStrengthActivity" />
        <activity android:name=".ViewPastWorkoutActivity" />
        <activity android:name=".LogWorkoutActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".FitnessActivity" />
        <activity android:name=".LeaderboardActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>